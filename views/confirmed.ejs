<!DOCTYPE html>
<html>
  <% include partials/header %>

<body>
  <% include partials/sidenav %>
  
  <div id="main"> <!-- main part is necessary for sidenav -->

  <% include partials/nav %>
  <!-- [confirmed.ejs] page starts from here -->
  <style>
    .msg{
      height:100%;
      min-height:800px;
      width:100%;
      display:flex;
      flex-direction:column;
      justify-content:center;
      align-items:center;
    }
    .msg a{
      background-color:#3366FF;
      padding:20px;
      border-radius:4px;
      color:white;
      border:none;
      transition:0.5s;
      margin:30px;
      font-size:25px;
      text-decoration:none;
    }

    .msg a:hover{
      opacity:0.7;
      transition:0.5s;
    }
  </style>
    <div class="msg">
      <h1><%= msgtitle %></h1>
      <p><%= msgdetail %></p>
      <a href="/">첫 화면으로 돌아가기</a>
    </div>
  <!-- [confirmed.ejs] page ends from here -->

  </div>
  <!-- end of main -->

  <% include partials/footer %>
</body>
</html>